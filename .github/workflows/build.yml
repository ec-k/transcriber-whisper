name: build

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

env:
  ARTIFACT_NAME: transcriber-whisper-windows-x64

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v7

      - uses: astral-sh/setup-uv@v10.0.1
        with:
          enable-cache: true

      # Pulls the bundled CUDA runtime as well, so this is roughly 2GB.
      - name: Install dependencies
        run: uv sync --locked

      # `av.utils` is only imported from within PyAV's Cython modules, so the
      # import scan never sees it. The CUDA runtime has to go in as a raw dir
      # because `--include-data-dir` drops DLLs and would copy headers only.
      - name: Compile with Nuitka
        run: >
          uv run --with "nuitka>=4.1.3" python -m nuitka
          --standalone
          --assume-yes-for-downloads
          --msvc=latest
          --output-dir=build
          --output-filename=transcriber-whisper.exe
          --include-module=av.utils
          --include-package-data=faster_whisper
          --include-raw-dir=.venv/Lib/site-packages/nvidia=nvidia
          src/transcriber_whisper/__main__.py

      - name: Locate the standalone directory
        shell: bash
        run: echo "DIST_DIR=$(ls -d build/*.dist | head -n 1)" >> "$GITHUB_ENV"

      # Also covers the CUDA DLL registration done at package import time.
      - name: Smoke test
        run: |
          & "$env:DIST_DIR\transcriber-whisper.exe" --help

      - name: Package
        run: 7z a -tzip "${{ env.ARTIFACT_NAME }}.zip" "$env:DIST_DIR\*"

      - uses: actions/upload-artifact@v7
        with:
          name: ${{ env.ARTIFACT_NAME }}
          path: ${{ env.ARTIFACT_NAME }}.zip
          compression-level: 0

      - name: Attach to release
        if: startsWith(github.ref, 'refs/tags/')
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ github.ref_name }}
        run: |
          gh release view "$TAG" >/dev/null 2>&1 || gh release create "$TAG" --verify-tag --generate-notes
          gh release upload "$TAG" "$ARTIFACT_NAME.zip" --clobber
